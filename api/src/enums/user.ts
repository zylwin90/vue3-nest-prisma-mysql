/**
 * @description: 用户状态
 */
export enum UserStaus {
    ALL = 0,
    /**
     * 正常
     */
    Normal = 1,
     /**
     * 禁用
     */
    Disable = 2,
}
/**
 * @description: 用户在线状态
 */
export enum UserLineStaus {
    // 全部
    ALL = 0,
     /**
     * 在线
     */ 
    Online = 1,
     /**
     * 下线
     */ 
    Offline = 2,
}
